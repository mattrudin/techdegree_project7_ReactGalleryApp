import React from 'react';

const NotFound = props => (
    <div>
        <h1 className="title">Page not found</h1>
        <h2>Seems you did something that wasn't planned...</h2>
    </div>
);

export default NotFound;